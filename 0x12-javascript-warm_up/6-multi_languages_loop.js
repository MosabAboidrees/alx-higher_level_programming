#!/usr/bin/node
// Create an array containing the three lines to print
const messages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loop through each element in the messages array
for (let i = 0; i < messages.length; i++) {
  // Print the current element of the messages array
  console.log(messages[i]);
}
