#!/usr/bin/node
// Define an object with type and value properties
const myObject = {
  type: 'object',
  value: 12
};
// Print the object before modification
console.log(myObject);
// Update the value property of the object to 89
myObject.value = 89;
// Print the object after modification
console.log(myObject);
